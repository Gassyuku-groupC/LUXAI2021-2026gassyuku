# Lux Role Replay Viewer

Open `index.html` directly in a browser. Load a converted stateful replay and
the matching `*.roles.json` sidecar generated by
`scripts/generate_deployed_agent_replays.ps1`.

The viewer does not modify the replay. Unit role rings and city role tile
overlays can be toggled independently while the base board remains visible.
