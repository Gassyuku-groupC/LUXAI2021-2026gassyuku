defaults:
  - conv_teacher_bc_dagger_v10_shadow
  - _self_

name: conv_teacher_bc_dagger_v11c_from_v10_replay_ready
group: local_teacher_bc_dagger_v11c_from_v10_replay_ready

reward_space_kwargs:
  # Conservative learning-layer tweak for the next probe: keep V10's safety
  # shaping, add only a small early research signal, and avoid the V11 midgame
  # buffer term that worsened night city loss.
  small_map_research: 0.04

hydra:
  run:
    dir: outputs/teacher_bc_dagger_v11c_from_v10_replay_ready/${now:%Y-%m-%d_%H-%M-%S}
