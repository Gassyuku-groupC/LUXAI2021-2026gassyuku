defaults:
  - conv_teacher_bc_dagger_v9_robustness
  - _self_

name: conv_teacher_bc_dagger_v10_shadow
group: local_teacher_bc_dagger_shadow

# V10 changes learner lifecycle, not the stable V9 reward weights. The reward
# implementation now checks every newly added city-tile coordinate, including
# expansions that merge into an existing city id.
total_games: 16
total_steps: 6400

hydra:
  run:
    dir: outputs/teacher_bc_dagger_v10_shadow/${now:%Y-%m-%d_%H-%M-%S}
